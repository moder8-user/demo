/* eslint-disable prettier/prettier */
export enum Role {
  ADMIN = 'admin',
  MANAGER = 'manager',
  MEMBER = 'member',
}

export enum Status {
  AVAILABLE = 'available',
  PENDING = 'pending',
  BORROWED = 'borrowed',
}
